PUBLIC_BASE_URL=http://localhost:5173
PORT=8787
HOST=127.0.0.1
DATABASE_URL=file:./data/bridge.db
ENCRYPTION_KEY=
APP_SECRET=
LINEAR_LIVE=false
LOG_LEVEL=info

# --- Docker compose: api-key profile only ---
# Required when running `docker compose --profile api-key up`.
# Leave blank for local development or when using the local-auth profile.
ANTHROPIC_API_KEY=
